openapi: "3.0.0"
info:
  title: "Picture-Grid API"
  version: "1.0.0"
  description: "REST API for the communication between the Picture-Grid server and client"

servers:
  - url: http://localhost:8080
    description: Local
  - url: /picture-grid-server/api
    description: Containerized

paths:
  /picture:
    get:
      operationId: getPictures
      summary: Get all pictures
      tags:
        - Picture
      responses:
        200:
          description: Successful
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PictureDescriptionDTO'

    post:
      operationId: createPicture
      summary: Create a picture
      tags:
        - Picture
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PictureTemplateDTO'
      responses:
        201:
          description: 'Created successfully'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PictureDescriptionDTO'
        
  /picture/{id}:
    get:
      operationId: getPicture
      summary: Get the picture with the given id
      tags:
        - Picture
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the picture to download.
          schema:
            type: string
            format: uuid
            minimum: 1
      responses:
        200:
          description: Found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PictureDescriptionDTO'
        404:
          description: Not found

    put:
      operationId: updatePictureDescription
      summary: Update the picture description with the given id
      tags:
        - Picture
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the picture to download.
          schema:
            type: string
            format: uuid
            minimum: 1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PictureDescriptionDTO'
      responses:
        204:
          description: Updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PictureDescriptionDTO'
        404:
          description: Item to update not found

    delete:
      operationId: deletePicture
      summary: Deletes the picture with the given id
      tags:
        - Picture
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the picture to delete.
          schema:
            type: string
            format: uuid
            minimum: 1
      responses:
        204:
          description: 'Succesfully deleted'
        404:
          description: 'Not found'

  /picture/file/{id}:
    get:
      operationId: downloadPicture
      summary: Download the picture with the given id
      tags:
        - Picture
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the picture to download.
          schema:
            type: string
            format: uuid
            minimum: 1
      responses:
        '200':
          description: Found
          content:
            application/json:
              schema:
               $ref: '#/components/schemas/PictureDTO'
    put:
      operationId: updatePicture
      summary: Update picture file with the given id
      tags:
        - Picture
      parameters:
        - name: id
          in: path
          required: true
          description: The id of the picture to download.
          schema:
            type: string
            format: uuid
            minimum: 1
      responses:
        '200':
          description: Found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PictureDTO'

components:
  schemas:
    PictureDescriptionDTO:
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        link:
          type: string
    PictureDTO:
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
        picture:
          type: string
          format: binary
    PictureTemplateDTO:
      properties:
        title:
          type: string
        description:
          type: string
        link:
          type: string
        picture:
          type: string
          format: binary
